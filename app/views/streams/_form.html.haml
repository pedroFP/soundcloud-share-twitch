= form_with(model: stream, local: true) do |form|
  - if stream.errors.any?
    #error_explanation
      %h2
        = pluralize(stream.errors.count, "error")
        prohibited this stream from being saved:
      %ul
        - stream.errors.full_messages.each do |message|
          %li= message
  .field
    = form.label :title
    = form.text_field :title
  .field
    = form.label :aired_at
    = form.datetime_select :aired_at
  .field
    = form.label :receiving_tracks
    = form.check_box :receiving_tracks
  .actions
    = form.submit
